apiVersion: v1
kind: Service
metadata:
  name: __APP__-svc
  namespace: __NAMESPACE__
  labels:
    app.kubernetes.io/name: __APP__
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: __SERVER_PORT__
  selector:
    app: __APP__
