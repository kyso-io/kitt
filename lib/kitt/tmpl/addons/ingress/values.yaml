config:
  enable-brotli: "true"
  use-gzip: "true"
ingressClassResource:
  default: true
extraArgs:
  default-ssl-certificate: "ingress/__INGRESS_CERT_NAME__"
  ingress-class: nginx
replicaCount: __REPLICAS__
